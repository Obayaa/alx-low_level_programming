#include <stdio>

/**
 * main - main function
 *
 * Return: always 0
 */
int main(void)
{
	char 1;

	for(i = 'a' ; i <= 'z' ; i++)
		if (i != q && i != e)
			putchar(i);
	putchar('\n');
	return (0);
}
